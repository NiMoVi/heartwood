---
phase: 02-content-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/screens.jsx
  - src/components.jsx
  - src/css/kit.css
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05

must_haves:
  truths:
    - "Clicking 'About' in the header navigates to the About screen"
    - "About screen renders Nico's bio covering Army Reserve, FIU, and Deloitte arc"
    - "About screen has a trilingual callout showing English, Spanish, Portuguese as chips"
    - "About screen has a certifications grid displaying all 5 certs with name, issuer, and year"
    - "About screen has a resume download anchor pointing to assets/resume/Nicolas_Mora_Resume.pdf"
    - "Field Notes link is removed from the header nav (deferred to v2)"
    - "'about' view string is handled by App render branch"
  artifacts:
    - path: "src/screens.jsx"
      provides: "About component and App render branch"
      contains: "function About"
    - path: "src/components.jsx"
      provides: "Header with updated nav links"
      contains: "'about', 'About'"
    - path: "src/css/kit.css"
      provides: "About page CSS classes"
      contains: ".about-bio"
  key_links:
    - from: "src/components.jsx Header links array"
      to: "src/screens.jsx App go('about')"
      via: "onClick={() => go('about')"
      pattern: "go\\('about'\\)"
    - from: "src/screens.jsx App render"
      to: "About component"
      via: "{view === 'about' && <About go={go} />}"
      pattern: "view === 'about'"
    - from: "src/screens.jsx About certifications section"
      to: "src/data.js CERTS window global"
      via: "CERTS.map(...)"
      pattern: "CERTS\\.map"

user_setup:
  - service: resume-pdf
    why: "Resume download button (ABOUT-05) links to assets/resume/Nicolas_Mora_Resume.pdf which does not yet exist"
    required_action: "Convert _Nicolas_Mora_Resume.docx to PDF and commit as assets/resume/Nicolas_Mora_Resume.pdf. Until then, the download button renders but the link will 404."
    workaround: "Code ships with the correct href. Add a <!-- TODO: add PDF --> comment. The .docx at assets/resume/_Nicolas_Mora_Resume.docx can be used as a temporary stand-in if needed."
---

<objective>
Build the About screen: bio narrative, trilingual callout, certifications grid, and resume download button. Wire it into the Header nav and App render loop. Add the necessary CSS classes to kit.css.

Purpose: There is currently no About page. Navigating to 'about' would render nothing. This plan adds the complete About screen as a new function component in screens.jsx, wires it into the App state machine, adds the 'About' link to the Header (replacing the deferred 'Field Notes' link), and adds the CSS required to render the bio, certifications grid, and language chips correctly.

Output:
- `src/screens.jsx` — new `About` component + `{view === 'about' && <About go={go} />}` render branch
- `src/components.jsx` — Header `links` array updated: `['notes', 'Field notes']` replaced by `['about', 'About']`
- `src/css/kit.css` — new CSS block for `.about-bio`, `.about-langs`, `.cert-grid`, `.cert-card`, `.cert-name`, `.cert-meta`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Do not explore the codebase — use these directly. -->

From src/screens.jsx — App function current render block (lines 153–157):
```jsx
{view === 'home'    && <Home go={go} openProject={openProject} />}
{view === 'project' && project && <ProjectDetail p={project} go={go} openProject={openProject} />}
{view === 'notes'   && <FieldNotes />}
{view === 'contact' && <Contact />}
```
Add after the contact branch: `{view === 'about' && <About go={go} />}`

From src/components.jsx — Header links array (line 25):
```jsx
const links = [['home', 'Work'], ['notes', 'Field notes'], ['contact', 'Contact']];
```
Replace with:
```jsx
const links = [['home', 'Work'], ['about', 'About'], ['contact', 'Contact']];
```

From src/data.js — CERTS array (already correct, no changes needed):
```js
const CERTS = [
  { name: 'Azure Data Engineer Associate', issuer: 'Microsoft',  year: '2024' },
  { name: 'Google Cloud Digital Leader',   issuer: 'Google',     year: '2023' },
  { name: 'Tableau Desktop Specialist',    issuer: 'Salesforce', year: '2023' },
  { name: 'Google Data Analyst',           issuer: 'Google',     year: '2022' },
  { name: 'Claude Code in Action',         issuer: 'Anthropic',  year: '2025' },
];
```

From src/components.jsx — existing primitives available in About component:
```jsx
// MonoLabel — section eyebrow label
function MonoLabel({ children, line = true }) { ... }

// Icon — Lucide icon, already wired to refreshIcons() via App useEffect
function Icon({ name, size = 20, className = '', stroke = 1.75 }) { ... }
```

Existing CSS patterns to follow (from kit.css):
```css
.chip { font-family: var(--font-mono); font-size: 11px; padding: 4px 10px;
  border-radius: var(--radius-pill); border: 1px solid var(--border-strong); color: var(--fg-muted); }
.section { padding: 72px 0; }
.sec-head { display: flex; align-items: flex-end; justify-content: space-between; margin-bottom: 40px; }
.sec-head h2 { font-family: var(--font-display); font-weight: 400; font-size: 39px; letter-spacing: -0.02em; margin: 14px 0 0; }
```

PITFALL: Do NOT nest `<button>` inside `<a>` for the resume download. Use a plain `<a>` with `className="btn btn-primary"` directly — the `.btn.btn-primary` CSS classes are already defined in kit.css and work on any element.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add About CSS classes to kit.css</name>
  <files>src/css/kit.css</files>
  <read_first>
    - src/css/kit.css — read the full file; append new CSS block after the existing `/* ---------- field notes ---------- */` block and before the `/* ---------- contact / footer ---------- */` block
    - .planning/phases/02-content-pages/02-RESEARCH.md — "New CSS for About Page" section provides the exact CSS to use
  </read_first>
  <action>
In `src/css/kit.css`, append the following CSS block. Insert it between the `/* ---------- field notes ---------- */` section and the `/* ---------- contact / footer ---------- */` section. Do not modify any existing CSS.

```css
/* ---------- about page ---------- */
.about-bio { max-width: 62ch; }
.about-bio p {
  font-size: 17px; line-height: 1.75; color: var(--fg-2); margin: 0 0 20px;
}
.about-langs {
  display: flex; align-items: center; gap: 10px; margin-top: 28px; flex-wrap: wrap;
}
.about-langs .mono-label { margin-right: 4px; }

.cert-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 16px;
}
.cert-card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius-md); padding: 18px 20px;
  display: flex; flex-direction: column; gap: 6px;
  transition: border-color var(--dur-fast) var(--ease-out);
}
.cert-card:hover { border-color: var(--border-strong); }
.cert-name { font-size: 15px; color: var(--fg); font-weight: 500; }
.cert-meta {
  font-family: var(--font-mono); font-size: 11px; color: var(--fg-muted);
  letter-spacing: 0.08em;
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/css/kit.css','utf8'); const checks=['.about-bio','.about-langs','.cert-grid','.cert-card','.cert-name','.cert-meta']; let pass=true; checks.forEach(s=>{if(!c.includes(s)){console.error('FAIL: missing CSS class '+s); pass=false;}}); if(pass) console.log('PASS — all About CSS classes present'); process.exit(pass?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/css/kit.css` contains `.about-bio` rule
    - `src/css/kit.css` contains `.about-langs` rule
    - `src/css/kit.css` contains `.cert-grid` rule with `grid-template-columns`
    - `src/css/kit.css` contains `.cert-card` rule with `background: var(--surface)`
    - `src/css/kit.css` contains `.cert-name` rule
    - `src/css/kit.css` contains `.cert-meta` rule with `font-family: var(--font-mono)`
    - Existing CSS rules (`.notes-list`, `.contact`, `.footer`) are unchanged
  </acceptance_criteria>
  <done>About page CSS classes exist in kit.css, ready for the About component to render against them.</done>
</task>

<task type="auto">
  <name>Task 2: Build About component, update Header nav, wire App render</name>
  <files>src/screens.jsx, src/components.jsx</files>
  <read_first>
    - src/screens.jsx — read the full file; add the About function before the App function; add the view branch inside App
    - src/components.jsx — read the full file; update Header links array only
    - .planning/phases/02-content-pages/02-RESEARCH.md — "About Screen Skeleton", "Connecting About to App Navigation", bio narrative section, and pitfalls (especially Pitfall 1 re: button-in-anchor)
  </read_first>
  <action>
**Edit 1 — src/screens.jsx:**

Add the following `About` function immediately before the `App` function (insert between the closing `}` of `Contact` and the opening `function App()`). Do not modify any existing functions.

```jsx
function About({ go }) {
  return (
    <React.Fragment>
      <section className="section wrap">
        <div className="sec-head">
          <div>
            <div className="rise d1"><MonoLabel>About</MonoLabel></div>
            <h2 className="rise d2">The story so far.</h2>
          </div>
          <a href="assets/resume/Nicolas_Mora_Resume.pdf"
             download="Nicolas_Mora_Resume.pdf"
             className="btn btn-primary rise d2">
            Download Resume <Icon name="download" size={17} />
          </a>
        </div>
        <div className="about-bio rise d3">
          <p>I'm a Senior Consultant in AI &amp; Analytics at Deloitte, where I've spent the last three years building the data systems that actually run large enterprises — not the slides that describe them. My work spans data pipeline architecture, agentic AI workflows, and BI platforms deployed across federal health, financial services, and retail clients.</p>
          <p>Before Deloitte, I served as a Field Artillery officer in the U.S. Army Reserve from 2013 to 2019. That background — planning under uncertainty, leading technical teams, executing against hard deadlines — shapes how I approach engineering work. I studied International Business at Florida International University, graduating Cum Laude, and then moved into data engineering as AI started becoming a real part of enterprise infrastructure.</p>
          <p>What makes my work distinct is that I build and I consult. I write Python and SQL every day. I've shipped production Synapse pipelines, designed agentic document review systems using Claude, and led BI modernization programs for organizations with millions of users. I'm not a generalist who knows a little about everything — I'm a data and AI specialist who understands the full stack from warehouse to dashboard to autonomous agent.</p>
          <div className="about-langs">
            <MonoLabel line={false}>Languages</MonoLabel>
            <span className="chip">English</span>
            <span className="chip">Spanish</span>
            <span className="chip">Portuguese</span>
          </div>
        </div>
      </section>
      <section className="section wrap">
        <div className="sec-head">
          <div>
            <div className="rise d1"><MonoLabel>Certifications</MonoLabel></div>
            <h2 className="rise d2">Credentials</h2>
          </div>
        </div>
        <div className="cert-grid rise d3">
          {CERTS.map(c => (
            <div key={c.name} className="cert-card">
              <span className="cert-name">{c.name}</span>
              <span className="cert-meta">{c.issuer} · {c.year}</span>
            </div>
          ))}
        </div>
      </section>
    </React.Fragment>
  );
}
```

In the `App` function's return JSX, add the About render branch immediately after the contact branch:
```jsx
{view === 'contact' && <Contact />}
{view === 'about'   && <About go={go} />}
```

**Edit 2 — src/components.jsx:**

In the `Header` function, update the `links` array (line 25). Replace:
```jsx
const links = [['home', 'Work'], ['notes', 'Field notes'], ['contact', 'Contact']];
```
With:
```jsx
const links = [['home', 'Work'], ['about', 'About'], ['contact', 'Contact']];
```

Do not change any other part of components.jsx.

**Key constraints:**
- The resume download anchor uses `className="btn btn-primary"` directly on the `<a>` tag — do NOT wrap a `<Button>` component inside an `<a>` (invalid HTML nesting)
- `CERTS` is a window global set in data.js; use it directly, no import needed
- The `go` prop is passed to About but is not required in the current implementation (included for future use and consistency with other screens)
- The `<MonoLabel line={false}>` call disables the decorative line for the inline languages label
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/screens.jsx','utf8'); const c=fs.readFileSync('src/components.jsx','utf8'); const checks=[[s,'function About','screens.jsx missing About function'],[s,'view === \'about\'','screens.jsx missing about render branch'],[s,'CERTS.map','screens.jsx missing CERTS render'],[s,'about-bio','screens.jsx missing about-bio class'],[s,'cert-grid','screens.jsx missing cert-grid class'],[s,'English','screens.jsx missing language chip'],[s,'Spanish','screens.jsx missing language chip'],[s,'Portuguese','screens.jsx missing language chip'],[s,'Nicolas_Mora_Resume.pdf','screens.jsx missing resume href'],[s,'Army Reserve','screens.jsx missing bio text'],[s,'Deloitte','screens.jsx missing bio text'],[c,\"'about', 'About'\",'components.jsx Header missing About link'],[c,'Field notes',false]]; let pass=true; checks.forEach(([source,str,msg])=>{if(typeof str==='boolean'){/* skip */return;}const found=source.includes(str); if(!found){console.error('FAIL: '+msg); pass=false;}}); const noFieldNotes=!c.includes('Field notes'); if(!noFieldNotes){console.error('FAIL: components.jsx still contains Field notes link'); pass=false;} if(pass) console.log('PASS — About component wired correctly'); process.exit(pass?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens.jsx` contains `function About(`
    - `src/screens.jsx` contains `view === 'about'` in App render
    - `src/screens.jsx` About component contains `CERTS.map(`
    - `src/screens.jsx` About component contains bio text mentioning `Army Reserve`
    - `src/screens.jsx` About component contains bio text mentioning `Deloitte`
    - `src/screens.jsx` About component contains `FIU` or `Florida International University`
    - `src/screens.jsx` About component contains language chips: `English`, `Spanish`, `Portuguese`
    - `src/screens.jsx` About component contains `href="assets/resume/Nicolas_Mora_Resume.pdf"` on an `<a>` element
    - `src/screens.jsx` About component does NOT use `<Button>` wrapped inside `<a>` (no `<Button` within 200 chars of `Nicolas_Mora_Resume.pdf`)
    - `src/screens.jsx` About component renders `cert-grid` div with `CERTS.map`
    - `src/components.jsx` Header links contains `['about', 'About']`
    - `src/components.jsx` Header links does NOT contain `Field notes`
    - `src/components.jsx` has no other changes (WorkCard, Footer, LinkedInIcon, etc. unchanged)
  </acceptance_criteria>
  <done>About screen is fully built and wired: bio narrative (Army→FIU→Deloitte), trilingual chip callout, 5-cert grid, resume download anchor. Header nav shows "About" instead of "Field notes". Clicking About in the header navigates to the About screen.</done>
</task>

</tasks>

<verification>
Open `index.html` in a browser:
1. Click "About" in the header nav — the About screen should render (not blank, not an error)
2. Bio section: should show 3 paragraphs covering Army Reserve, FIU, and Deloitte work
3. Language chips: "English", "Spanish", "Portuguese" should appear as chip elements
4. Certifications grid: should show all 5 cert cards (Azure Data Engineer Associate, Google Cloud Digital Leader, Tableau Desktop Specialist, Google Data Analyst, Claude Code in Action)
5. "Download Resume" button should be visible — clicking will 404 until the PDF is committed (expected behavior until Nico exports the PDF)
6. Clicking "Work" or "Nico Mora" in the header should return to the home view

Check browser console — no errors about undefined CERTS or missing components.
</verification>

<success_criteria>
The About screen is accessible from the header, shows Nico's professional narrative (Army → FIU → Deloitte), surfaces trilingual capability via chips, displays all 5 certifications in a grid, and has a resume download button wired to the correct PDF path. Header nav reads: Work · About · Contact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-03-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
