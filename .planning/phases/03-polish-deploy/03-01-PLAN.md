---
phase: 03-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nojekyll
autonomous: false
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "Visiting https://nimovi.github.io/heartwood-NicoMora/ in a browser loads the portfolio with no 404"
    - "All assets (CSS, JSX, fonts) load without console errors on the live URL"
    - "GitHub Pages is configured to serve from the root of main branch"
  artifacts:
    - path: ".nojekyll"
      provides: "Prevents Jekyll from mangling CDN React/Babel files on GitHub Pages"
      contains: "(empty file)"
  key_links:
    - from: "GitHub Pages Settings"
      to: "main branch / (root)"
      via: "Settings -> Pages -> Deploy from branch"
      pattern: "Branch: main, Folder: /(root)"
    - from: ".nojekyll"
      to: "GitHub Pages build"
      via: "presence of file disables Jekyll processing"
      pattern: ".nojekyll exists at repo root"
---

<objective>
Enable GitHub Pages on the NiMoVi/heartwood-NicoMora repository and verify the live site loads without errors.

Purpose: The portfolio cannot be shared until it is publicly accessible. This is the prerequisite for all other Phase 3 work — the confirmed live URL must be known before OG absolute URLs can be written.

Output: Live site at https://nimovi.github.io/heartwood-NicoMora/ with no 404s or broken assets. Confirmed base URL for Plan 03-03 to use in OG meta tags.

NOTE ON URL: The git remote is NiMoVi/heartwood-NicoMora (not NiMoVi/heartwood as REQUIREMENTS.md originally stated). The live URL will therefore be https://nimovi.github.io/heartwood-NicoMora/ — not https://nimovi.github.io/heartwood/. REQUIREMENTS.md DEPLOY-01 language references the old repo name; the actual live URL is what matters for success. If the user wants to rename the repo to "heartwood" to match the requirement text exactly, that is a separate optional step (rename in GitHub Settings -> General -> Repository name -> heartwood). Either way, this plan activates Pages first, then verifies the URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-deploy/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .nojekyll at repo root</name>
  <files>.nojekyll</files>
  <read_first>
    - index.html (confirm it exists at repo root — it does; .nojekyll goes in same directory)
    - .planning/phases/03-polish-deploy/03-RESEARCH.md (Pitfall 2: Jekyll processing section)
  </read_first>
  <action>
    Create an empty file named exactly `.nojekyll` at the repository root (c:/devwork/heartwood/.nojekyll). The file must be completely empty — no content, no newline. This file signals GitHub Pages to skip Jekyll processing and serve files as-is, preventing Jekyll from ignoring or mangling the CDN React/Babel JSX files.

    After creating the file, commit it immediately so it is present in the next push:
    - Staged file: .nojekyll
    - Commit message: "chore(03-01): add .nojekyll to disable Jekyll on GitHub Pages"

    Do NOT add any content to .nojekyll — it is an empty marker file by convention.
  </action>
  <verify>
    <automated>Test-Path "c:/devwork/heartwood/.nojekyll"</automated>
  </verify>
  <acceptance_criteria>
    - .nojekyll file exists at c:/devwork/heartwood/.nojekyll
    - File is empty (0 bytes)
    - File is committed to the main branch (appears in git log)
    - No Jekyll-related files (.gemspec, _config.yml) exist at repo root
  </acceptance_criteria>
  <done>.nojekyll exists at repo root, is empty, and is committed to main</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Activate GitHub Pages in repo settings</name>
  <files>N/A — browser UI action; no files modified by this task</files>
  <what-built>
    .nojekyll was committed to main. GitHub Pages must now be enabled via the repository Settings UI — this is a one-time browser UI action that cannot be automated without the gh CLI (not installed).
  </what-built>
  <action>
    This task requires manual browser interaction. Push the .nojekyll commit to origin/main, then enable GitHub Pages in the repository Settings UI:

    1. Run: git push origin main
    2. Open https://github.com/NiMoVi/heartwood-NicoMora in a browser
    3. Click Settings (top tab, gear icon)
    4. In the left sidebar, click Pages (under "Code and automation")
    5. Under "Build and deployment", set Source to "Deploy from a branch"
    6. Set Branch to "main" and Folder to "/ (root)"
    7. Click Save
    8. Wait 2-10 minutes for deployment. Refresh Settings -> Pages until a green "Your site is live" banner appears.
    9. Visit the confirmed live URL and verify it loads.

    OPTIONAL REPO RENAME: If you prefer https://nimovi.github.io/heartwood/ (matching REQUIREMENTS.md text), rename the repo first via Settings -> General -> Repository name -> "heartwood" -> Rename. All absolute URLs in Plan 03-03 must then use that base instead. Decide BEFORE completing Plan 03-03.
  </action>
  <how-to-verify>
    1. Push all commits to origin/main: `git push origin main`
    2. Open https://github.com/NiMoVi/heartwood-NicoMora in a browser
    3. Click Settings -> Pages
    4. Set Source: Deploy from branch, Branch: main, Folder: /(root) -> Save
    5. Wait 2-10 minutes. Refresh until green banner: "Your site is live at https://nimovi.github.io/heartwood-NicoMora/"
    6. Visit https://nimovi.github.io/heartwood-NicoMora/ in a browser
    7. Confirm: portfolio loads, no 404, CSS renders (dark background, green accent), no browser console errors
    8. Optional: Check https://github.com/NiMoVi/heartwood-NicoMora/actions for "pages build and deployment" success
  </how-to-verify>
  <verify>
    <automated>MISSING — GitHub Pages activation is a UI action; verification is manual (visit live URL in browser)</automated>
  </verify>
  <done>GitHub Pages is live; confirmed URL is recorded in 03-01-SUMMARY.md for use by Plan 03-03</done>
  <resume-signal>
    Type "live" and the confirmed URL (e.g., "live https://nimovi.github.io/heartwood-NicoMora/") when the site loads without 404. If you renamed the repo, include the new URL. This URL will be used verbatim in Plan 03-03 OG meta tags.
  </resume-signal>
</task>

</tasks>

<verification>
After Task 2 completes:
- https://nimovi.github.io/heartwood-NicoMora/ (or renamed URL) returns HTTP 200
- Page renders with dark background and green accent (CSS loaded)
- Browser console shows no 404 errors for CSS, JSX, or script assets
- GitHub Settings -> Pages shows green "Your site is live" banner
</verification>

<success_criteria>
1. .nojekyll file exists at repo root, is empty, and is committed
2. GitHub Pages is enabled, pointing to main branch / (root) folder
3. The live URL is confirmed and recorded — executor must note the final URL in the SUMMARY so Plan 03-03 can reference it
4. Visiting the live URL renders the portfolio without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-deploy/03-01-SUMMARY.md`

The SUMMARY must include the confirmed live base URL (e.g., https://nimovi.github.io/heartwood-NicoMora/) in a clearly labeled field so Plan 03-03 can use it verbatim in og:url and og:image meta tags.
</output>
